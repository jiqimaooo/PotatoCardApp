// 团队共享签名配置。
// 个人 Team ID 和 Bundle ID 请写入同目录的 Signing.local.xcconfig。

CODE_SIGN_STYLE = Automatic

POTATO_DEVELOPMENT_TEAM =
APP_BUNDLE_IDENTIFIER = com.sunbelife.potatocard.app
WIDGET_BUNDLE_IDENTIFIER = $(APP_BUNDLE_IDENTIFIER).transfer-live-activity-widget

#include? "Signing.local.xcconfig"
