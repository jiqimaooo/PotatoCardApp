// 复制为 Signing.local.xcconfig 后按本机 Apple Developer 账号修改。
// Signing.local.xcconfig 已被 git 忽略，不要提交。

POTATO_DEVELOPMENT_TEAM = YOURTEAMID
APP_BUNDLE_IDENTIFIER = com.example.potatocard.dev
WIDGET_BUNDLE_IDENTIFIER = $(APP_BUNDLE_IDENTIFIER).TransferLiveActivityWidget
